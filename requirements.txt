# SEEG Manifold & Symmetry Analysis Toolkit — runtime dependencies
#
# Lower bounds reflect the versions the test suite has been run against.
# Every package listed here is imported by the code; optional analyses live
# in the extras below and in setup.py's extras_require.

# ---- Core ----
numpy>=1.24,<3.0
scipy>=1.10
scikit-learn>=1.3

# ---- Neural data IO ----
# mne < 1.6 calls np.cast, which NumPy 2.0 removed, so the lower bound
# is required rather than cosmetic.
mne>=1.6
h5py>=3.8               # MATLAB v7.3 (HDF5) files

# ---- Configuration ----
pyyaml>=6.0

# ---- Visualization ----
matplotlib>=3.7

# ---- Testing ----
pytest>=7.0

# ---- Optional -------------------------------------------------------------
# Install with:  pip install -e ".[full]"
#
# umap-learn>=0.5.4     # UMAP embedding      (manifold.fit_umap)
# phate>=1.0            # PHATE embedding     (manifold.fit_phate)
# ripser>=0.6           # persistence diagrams (topology)
# persim>=0.3           # diagram distances    (topology)
# plotly>=5.0           # interactive plots    (visualization.interactive)
